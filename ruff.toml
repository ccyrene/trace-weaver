# Ruff config for TraceWeaver. Defaults otherwise (line-length 88, etc.).
target-version = "py310"

# Lint scope is passed explicitly on the CLI (src tests ci); generated scan
# outputs and CI scanner reports are never linted.
extend-exclude = [
    "outputs",
    "reports",
]
